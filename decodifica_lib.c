#include "lista_lib.h"
#include "files_lib.h"
#include "cifrasBeale_lib.h"

struct cifrasBeale *leChaveCifra(char *arq)
{
    FILE *arqChave = leArquivo(arq);
    struct cifrasBeale *pt = criaCifras();

    return NULL;
}

int main()
{

    return 0;
}
